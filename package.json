{
  "name": "dbless",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "admin": "tsx cli/index.ts",
    "admin:local": "node --env-file=.env --import tsx cli/index.ts",
    "build:sdk": "tsc -p sdk/tsconfig.json",
    "dev:local": "node --env-file=.env --import tsx local/server.ts",
    "smoke:local": "node --env-file=.env --import tsx local/smoke.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.1"
  }
}
