# CLI only: R2 S3 API credentials with access to the bucket
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=dbless

# Worker: set with `npx wrangler secret put CURSOR_SECRET`
# Use a random 32-byte value or longer.
